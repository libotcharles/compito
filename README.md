# E-Commerce TPSI - Livello Hard
**Architettura**: Thin Client. La logica (controllo crediti/stock) è nel backend.
**Database**: Supabase (PostgreSQL).
**Endpoints**: GET `/api/data`, POST `/api/buy`, POST `/api/admin/update`.
**IA**: Usata per il debug della logica asincrona e del CSS.